import { SpaceAfterCommaPipe } from './space-after-comma.pipe';

describe('SpaceAfterCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceAfterCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
